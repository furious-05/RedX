{
  "commands": [
    {
      "title": "Nmap SMB Version Detection",
      "description": "Use Nmap to detect SMB version and OS details on port 445",
      "template": "nmap -sV -p 445 --script=smb-os-discovery {ip}"
    },
    {
      "title": "SMBclient Version Check",
      "description": "Use smbclient to check SMB version by connecting to the target",
      "template": "smbclient -L //{ip} -N"
    },
    {
      "title": "RPCclient OS Version Detection",
      "description": "Use rpcclient to query the OS version information",
      "template": "rpcclient -U \"\" {ip} -c 'srvinfo'"
    }
  ]
}
    